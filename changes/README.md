# Changes

Proposed specification changes land here as OpenSpec deltas, one directory per change (`<change-id>/proposal.md` + delta files). Accepted changes merge into `specs/` and move to `changes/archive/`.
